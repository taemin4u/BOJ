고장나지 않은 버튼들을 따로 모아 모든 경우에서 일일이 세려고 했음

막힌점
1. Base Case를 뭐로 정할 것인가
2. 같은 수가 중복되어 나오는 경우는 어떻게 할 것인가

해결
1. 몇자리까지 경우를 셀 것인지로 해결
2. 
for (int i = 0; i < v.size(); ++i) {
    Pick(cnt + 1, value * 10 + v[i], len);
}
이런식으로 하면 모든 경우에 대해 v[0]부터 시작하여 같은 수가 중복되어 나옴